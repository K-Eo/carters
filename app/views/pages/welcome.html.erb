<div id="landing-slider" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <% @sliders.each_with_index do |slider, index| %>
      <li data-target="landing-slider" data-slide-to="<%= index %>" class="<%= index == 0 ? 'active' : '' %>">
      </li>
      <% end %>
  </ol>
  <div class="carousel-inner">
    <% @sliders.each_with_index do |slider, index| %>
      <div class="carousel-item <%= index == 0 ? 'active' : '' %>">
        <img class="d-block w-100" src="http://via.placeholder.com/800x400?auto=yes&bg=777&fg=555&text=First slide" alt="First slide"
          height="350px">
      </div>
      <% end %>

  </div>
  <a class="carousel-control-prev" href="landing-slider" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="landing-slider" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<div class="container">
  <div class="row">
    <div class="col-12">
      <p class="text-right text-success"><%= link_to "Ver más", items_path %></p>
      
    </div>
  </div>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-3 itemCard">
        <div class="card">
          <img class="card-img-top" src="https://placehold.it/130x130" alt="Card image cap">
          <div class="card-body">
            <p class="card-text">
              <%= item.title %>
            </p>
            <span class="text-info">$<%= item.price %> MXN</span>
          </div>
        </div>
      </div>
      <% end %>
  </div>
</div>
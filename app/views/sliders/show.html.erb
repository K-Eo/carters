<div class="col-12">
  <p>
    <strong>Active:</strong>
    <%= @slider.active %>
  </p>
</div>

<div class="col-12">
  <p>
    <% if @slider.image.attached? %>
      <%= image_tag @slider.image.variant(resize: "150x90") %>
    <% end %>
  <p>
</div>

<div class="col-12">
  <%= link_to "Edit", edit_slider_path(@slider) %> |
  <%= link_to "Back", sliders_path %>
</div>

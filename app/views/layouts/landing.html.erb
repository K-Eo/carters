<!DOCTYPE html>
<html>
  <%= render "layouts/head" %>

  <body>
    <div class="wrapper">
      <%= render "shared/nav" %>

      <div class="app">
        <div class="content m-0 p-0">
          <%= render "layouts/messages" %>
          <%= yield %>
        </div>

        <%= render "shared/footer" %>
      </div>
    </div>
  </body>
</html>

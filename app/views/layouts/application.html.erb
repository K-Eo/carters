<!DOCTYPE html>
<html>
  <%= render "layouts/head" %>

  <body>
    <div class="wrapper">
      <%= render "shared/nav" %>

      <div class="app">
        <div class="content p-0">
          <%= render "layouts/messages" %>

          <div class="container-fluid m-0 p-0">
            <div class="row">
              <%= yield %>
            </div>
          </div>
        </div>

        <%= render "shared/footer" %>
      </div>
    </div>
  </body>
</html>

es-MX:
  simple_form:
    "yes": 'Si'
    "no": 'No'
    required:
      text: 'requerido'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      html: '<abbr title="requerido">*</abbr>'
    error_notification:
      default_message: "Por favor, revisa los siguientes errores:"
    hints:
    # Examples
    labels:
      defaults:
        category: 'Categoría'
      slider:
        image: Imagen
    hints:
      slider:
        active: Se muestra en la página de inicio cuando se marca.
        image: Dimensiones recomendadas 1920x853px.
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'
